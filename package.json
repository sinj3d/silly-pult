{
  "name": "sillyplut",
  "private": true,
  "scripts": {
    "dev:panel": "npm run dev --prefix control-panel",
    "dev:helper": "swift run --package-path helper SillypultHelper",
    "build:panel": "npm run build --prefix control-panel",
    "lint:panel": "npm run lint --prefix control-panel",
    "test:helper": "swift test --package-path helper"
  }
}
